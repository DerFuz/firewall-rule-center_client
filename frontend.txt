curl -fsSL https://deb.nodesource.com/gpgkey/nodesource-repo.gpg.key | sudo gpg --dearmor -o /etc/apt/keyrings/nodesource.gpg\n
NODE_MAJOR=20\necho "deb [signed-by=/etc/apt/keyrings/nodesource.gpg] https://deb.nodesource.com/node_$NODE_MAJOR.x nodistro main" | sudo tee /etc/apt/sources.list.d/nodesource.list
sudo apt update
sudo apt install nodejs

sudo apt install openjdk-13-jre

sudo npm install @openapitools/openapi-generator-cli -g

npx create-react-app frc-frontend --template typescript
sudo openapi-generator-cli generate -i http://127.0.0.1:8000/api/schema/\?format\=json -g typescript-axios -o src/components/api --additional-properties=supportsES6=true
sudo chown -R jakob:jakob components
npm install axios

# Applikationsumbegung installieren + Applikation starten
npm install
npm start